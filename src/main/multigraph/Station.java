package main.multigraph;

/**
 * Represents a Station in the Metro as a Node in a Graph.
 */
public class Station extends Node {
    public Station(int id, String name) {
        super(id, name);
    }
}
